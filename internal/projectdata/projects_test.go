package projectdata_test

import (
	"testing"
)

func TestParseJson(t *testing.T) {
	// logger := golog.New()
	// logger.AddHandler(golog.NewHandler(os.Stderr, golog.LevelTrace, golog.NewTextFormatter(golog.WithColor(true), golog.WithTimePrecision(3))))
	// projectdata.Update(`\\192.168.31.4\buffer\IN\@AMEDIA_IN\metadata.json`, logger)
}
